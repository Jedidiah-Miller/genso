/**
 * color and text defaults shared across the app
 */
export const Theme = {
  colors: {
    primaryColor: 'rgb(118, 158, 203)',
    secondaryColor: 'rbg(1, 158, 203)',
    destructiveColor: 'red', // change later
    warningColor: 'yellow' // change later
  }

}